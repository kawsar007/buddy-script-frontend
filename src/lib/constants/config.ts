

// export const serverConfig = {
//   apiBaseUrl: process.env.API_BASE_URL ?? "http://localhost:4000/api/v1",
//   accessCookieName: process.env.AUTH_ACCESS_COOKIE ?? "bs_access_token",
//   refreshCookieName: process.env.AUTH_REFRESH_COOKIE ?? "bs_refresh_token",
//   isProduction: process.env.NODE_ENV === "production",
// };

// export const publicConfig = {
//   appUrl: process.env.NEXT_PUBLIC_APP_URL ?? "http://localhost:3000",
//   googleClientId: process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID ?? "",
// };

// /** Access token lifetime used for cookie maxAge (in seconds). Mirrors backend expiry. */
// export const ACCESS_TOKEN_MAX_AGE = 60 * 15; // 15 minutes
// /** Refresh token lifetime (in seconds). */
// export const REFRESH_TOKEN_MAX_AGE = 60 * 60 * 24 * 30; // 30 days
// /** Shorter-lived session used when "Remember me" is unchecked. */
// export const SESSION_ONLY_REFRESH_MAX_AGE = 60 * 60 * 24; // 1 day
